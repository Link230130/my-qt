#ifndef WIDGETDELEGATE_H
#define WIDGETDELEGATE_H

#include <QObject>

class WidgetDelegate : public QObject
{
    Q_OBJECT
public:
    explicit WidgetDelegate(QObject *parent = nullptr);

signals:

public slots:
};

#endif // WIDGETDELEGATE_H
